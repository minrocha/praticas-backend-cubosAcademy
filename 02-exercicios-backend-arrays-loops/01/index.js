const numeros = [2, 3, 4, 6];
let soma = 0; // armazena soma dos itens do array

for (let numero of numeros) soma += numero; // percorre o array de numeros e soma o valor de cada índice a variavel soma

console.log(soma); // loga na tela o valor da variavel soma
